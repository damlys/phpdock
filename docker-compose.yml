version: "3.8"
services:
  base_image:
    build:
      context: base-image
      dockerfile: Dockerfile
    image: damlys/phpdock-base-image:0.0.0
    scale: 0

  app:
    depends_on:
      - base_image
    build:
      context: .
      dockerfile: Dockerfile
    image: damlys/phpdock-app:0.0.0
